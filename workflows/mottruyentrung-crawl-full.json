{
  "id": "crawl-mottruyentrung-full",
  "name": "Crawl mottruyentrung.net.vn (FULL — all manga)",
  "nodes": [
    {
      "parameters": {},
      "id": "trigger-1",
      "name": "Manual Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        200,
        300
      ]
    },
    {
      "parameters": {
        "assignments": {
          "assignments": [
            {
              "id": "a1",
              "name": "manga_limit",
              "value": 5,
              "type": "number"
            },
            {
              "id": "a2",
              "name": "chapters_per_manga_limit",
              "value": 0,
              "type": "number"
            },
            {
              "id": "a3",
              "name": "delay",
              "value": 1,
              "type": "number"
            }
          ]
        },
        "options": {}
      },
      "id": "set-1",
      "name": "Set Params",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        420,
        300
      ]
    },
    {
      "parameters": {
        "language": "python",
        "pythonCode": "import sys, re, urllib.request\n\nsys.path.insert(0, '/data/scripts')\nimport crawl_mottruyentrung as cm\n\nparams = _items[0]['json']\nmanga_limit = int(params.get('manga_limit') or 0)\nchapters_per_manga_limit = int(params.get('chapters_per_manga_limit') or 0)\ndelay = float(params.get('delay') or 1.0)\n\nxml = cm.http_request(f'{cm.BASE_URL}/wp-sitemap-posts-wp-manga-1.xml')\nlocs = re.findall(r'<loc>([^<]+)</loc>', xml)\nslugs = []\nseen = set()\nfor u in locs:\n    m = re.search(r'/manga/([a-z0-9-]+)/?', u)\n    if m and m.group(1) not in seen:\n        seen.add(m.group(1))\n        slugs.append(m.group(1))\n\nif manga_limit > 0:\n    slugs = slugs[:manga_limit]\n\nreturn [{'json': {\n    'slug': slug,\n    'index': i + 1,\n    'total': len(slugs),\n    'chapters_per_manga_limit': chapters_per_manga_limit,\n    'delay': delay,\n}} for i, slug in enumerate(slugs)]\n"
      },
      "id": "list-1",
      "name": "List Manga (sitemap)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        640,
        300
      ]
    },
    {
      "parameters": {
        "batchSize": 1,
        "options": {}
      },
      "id": "split-1",
      "name": "Loop Over Manga",
      "type": "n8n-nodes-base.splitInBatches",
      "typeVersion": 3,
      "position": [
        860,
        300
      ]
    },
    {
      "parameters": {
        "language": "python",
        "mode": "runOnceForEachItem",
        "pythonCode": "import sys, io, contextlib, re\nfrom pathlib import Path\n\nsys.path.insert(0, '/data/scripts')\nimport crawl_mottruyentrung as cm\n\nparams = _item['json']\nslug = params['slug']\nout_dir = Path('/data/crawl-output') / slug\ndelay = float(params.get('delay') or 1.0)\nlimit_raw = params.get('chapters_per_manga_limit')\nlimit = int(limit_raw) if limit_raw and int(limit_raw) > 0 else None\n\nbuf = io.StringIO()\ntry:\n    with contextlib.redirect_stdout(buf), contextlib.redirect_stderr(buf):\n        cm.crawl(slug, out_dir, delay, limit)\n    error = None\nexcept Exception as exc:\n    error = f'{type(exc).__name__}: {exc}'\n\nlog = buf.getvalue()\ntotal_match = re.search(r'Tìm thấy (\\d+) chương', log)\ncrawled = len(re.findall(r'^\\[>\\] ', log, flags=re.M))\nskipped = len(re.findall(r'^\\[=\\] ', log, flags=re.M))\nfailed = len(re.findall(r'^\\s*! Lỗi:', log, flags=re.M))\n\nreturn {\n    'success': error is None,\n    'error': error,\n    'slug': slug,\n    'index': params.get('index'),\n    'total': params.get('total'),\n    'out_dir': str(out_dir),\n    'total_chapters': int(total_match.group(1)) if total_match else None,\n    'crawled_now': crawled,\n    'skipped_existing': skipped,\n    'failed': failed,\n    'log_tail': log[-1000:],\n}\n"
      },
      "id": "crawler-1",
      "name": "Run Crawler (per manga)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1080,
        300
      ]
    },
    {
      "parameters": {
        "language": "python",
        "mode": "runOnceForEachItem",
        "pythonCode": "import os, re, sys, hmac, hashlib, datetime, unicodedata, urllib.request, urllib.parse\nfrom pathlib import Path\nfrom urllib.parse import urlparse\n\nsys.path.insert(0, '/data/scripts')\nimport crawl_mottruyentrung as cm\n\nprev = _item['json']\nif not prev.get('success'):\n    return {'success': False, 'error': 'crawler failed', 'crawler_result': prev}\n\nslug = prev['slug']\nout_dir = Path(prev['out_dir'])\n\ndef vn_slug(s):\n    s = unicodedata.normalize('NFKD', s)\n    s = ''.join(c for c in s if not unicodedata.combining(c))\n    s = s.replace('đ', 'd').replace('Đ', 'D').lower()\n    s = re.sub(r'\\s+', '-', s.strip())\n    s = re.sub(r'[^a-z0-9\\-_.]', '', s)\n    s = re.sub(r'-+', '-', s).strip('-.')\n    return s\n\ntitle = slug\ntry:\n    main_html = cm.http_request(f'{cm.BASE_URL}/manga/{slug}/')\n    m = re.search(r'<meta\\s+property=\"og:title\"[^>]*content=\"([^\"]+)\"', main_html, re.I)\n    if m:\n        title = m.group(1).strip()\nexcept Exception:\n    pass\n\nfolder = vn_slug(title) or slug\n\nendpoint = os.environ.get('MINIO_ENDPOINT', 'http://minio:9000')\naccess_key = os.environ['MINIO_ACCESS_KEY']\nsecret_key = os.environ['MINIO_SECRET_KEY']\nbucket = os.environ.get('MINIO_BUCKET', 'mottruyen')\nregion = os.environ.get('MINIO_REGION', 'us-east-1')\n\ndef s3_put(key, body, content_type, target_bucket=bucket):\n    parsed = urlparse(endpoint); host = parsed.netloc\n    now = datetime.datetime.now(datetime.UTC)\n    amzdate = now.strftime('%Y%m%dT%H%M%SZ'); datestamp = now.strftime('%Y%m%d')\n    payload_hash = hashlib.sha256(body).hexdigest()\n    canonical_uri = '/' + target_bucket + '/' + urllib.parse.quote(key, safe='/')\n    canonical_headers = f'host:{host}\\nx-amz-content-sha256:{payload_hash}\\nx-amz-date:{amzdate}\\n'\n    signed_headers = 'host;x-amz-content-sha256;x-amz-date'\n    canonical_request = f'PUT\\n{canonical_uri}\\n\\n{canonical_headers}\\n{signed_headers}\\n{payload_hash}'\n    algorithm = 'AWS4-HMAC-SHA256'\n    credential_scope = f'{datestamp}/{region}/s3/aws4_request'\n    string_to_sign = f'{algorithm}\\n{amzdate}\\n{credential_scope}\\n{hashlib.sha256(canonical_request.encode()).hexdigest()}'\n    def _sign(k, m): return hmac.new(k, m.encode(), hashlib.sha256).digest()\n    k_date = _sign(('AWS4' + secret_key).encode(), datestamp)\n    k_region = _sign(k_date, region); k_service = _sign(k_region, 's3'); k_signing = _sign(k_service, 'aws4_request')\n    signature = hmac.new(k_signing, string_to_sign.encode(), hashlib.sha256).hexdigest()\n    auth = f'{algorithm} Credential={access_key}/{credential_scope}, SignedHeaders={signed_headers}, Signature={signature}'\n    url = f'{endpoint}{canonical_uri}'\n    req = urllib.request.Request(url, data=body, method='PUT', headers={\n        'Host': host, 'X-Amz-Date': amzdate, 'X-Amz-Content-SHA256': payload_hash,\n        'Authorization': auth, 'Content-Type': content_type,\n    })\n    with urllib.request.urlopen(req, timeout=60) as resp:\n        return resp.status\n\nuploaded, errors = [], []\nfor f in sorted(out_dir.glob('*.txt')):\n    body = f.read_bytes()\n    key = f'{folder}/{f.name}'\n    try:\n        status = s3_put(key, body, 'text/plain; charset=utf-8')\n        uploaded.append({'key': key, 'size': len(body), 'status': status})\n    except Exception as e:\n        errors.append({'file': f.name, 'error': f'{type(e).__name__}: {e}'})\n\nreturn {\n    'success': len(errors) == 0 and len(uploaded) > 0,\n    'slug': slug,\n    'index': prev.get('index'),\n    'total': prev.get('total'),\n    'title': title,\n    'folder': folder,\n    'out_dir': str(out_dir),\n    'bucket': bucket,\n    'uploaded_count': len(uploaded),\n    'failed_count': len(errors),\n    'errors': errors,\n}\n"
      },
      "id": "upload-1",
      "name": "Upload to MinIO (per manga)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1300,
        300
      ]
    },
    {
      "parameters": {
        "language": "python",
        "mode": "runOnceForEachItem",
        "pythonCode": "import os, re, json, hmac, hashlib, datetime, urllib.request, urllib.parse\nfrom pathlib import Path\nfrom urllib.parse import urlparse\n\nprev = _item['json']\nif not prev.get('success'):\n    return {'success': False, 'error': 'upload failed', 'upload_result': prev}\n\nslug = prev.get('slug')\nfolder = prev['folder']\nout_dir = Path(prev['out_dir'])\n\nvllm_url = os.environ.get('VLLM_BASE_URL', 'http://vllm:8000/v1').rstrip('/') + '/chat/completions'\nvllm_key = os.environ.get('VLLM_API_KEY', 'vllm-local')\nmodel = os.environ.get('VLLM_MODEL', 'Qwen/Qwen3.6-35B-A3B-FP8')\n\nendpoint = os.environ.get('MINIO_ENDPOINT', 'http://minio:9000')\naccess_key = os.environ['MINIO_ACCESS_KEY']\nsecret_key = os.environ['MINIO_SECRET_KEY']\nbucket = 'mottruyen-refactored'\nregion = os.environ.get('MINIO_REGION', 'us-east-1')\n\ncache_dir = out_dir / '_refactored'\ncache_dir.mkdir(parents=True, exist_ok=True)\n\ndef call_vllm(text):\n    prompt = text + '\\n\\nchỉnh sửa lỗi chính tả của đoạn văn trên, chỉ trả về văn bản đã sửa, giữ nguyên định dạng và xuống dòng, không thêm giải thích.'\n    body = json.dumps({\n        'model': model,\n        'messages': [{'role': 'user', 'content': prompt}],\n        'temperature': 0.1,\n        'max_tokens': 20000,\n        'chat_template_kwargs': {'enable_thinking': False},\n    }).encode('utf-8')\n    req = urllib.request.Request(vllm_url, data=body, method='POST', headers={\n        'Content-Type': 'application/json',\n        'Authorization': f'Bearer {vllm_key}',\n    })\n    with urllib.request.urlopen(req, timeout=900) as r:\n        d = json.loads(r.read())\n    choice = d['choices'][0]\n    return (choice['message'].get('content') or '').strip(), choice.get('finish_reason')\n\ndef s3_put(key, body, content_type):\n    parsed = urlparse(endpoint); host = parsed.netloc\n    now = datetime.datetime.now(datetime.UTC)\n    amzdate = now.strftime('%Y%m%dT%H%M%SZ'); datestamp = now.strftime('%Y%m%d')\n    payload_hash = hashlib.sha256(body).hexdigest()\n    canonical_uri = '/' + bucket + '/' + urllib.parse.quote(key, safe='/')\n    canonical_headers = f'host:{host}\\nx-amz-content-sha256:{payload_hash}\\nx-amz-date:{amzdate}\\n'\n    signed_headers = 'host;x-amz-content-sha256;x-amz-date'\n    canonical_request = f'PUT\\n{canonical_uri}\\n\\n{canonical_headers}\\n{signed_headers}\\n{payload_hash}'\n    algorithm = 'AWS4-HMAC-SHA256'\n    credential_scope = f'{datestamp}/{region}/s3/aws4_request'\n    string_to_sign = f'{algorithm}\\n{amzdate}\\n{credential_scope}\\n{hashlib.sha256(canonical_request.encode()).hexdigest()}'\n    def _sign(k, m): return hmac.new(k, m.encode(), hashlib.sha256).digest()\n    k_date = _sign(('AWS4' + secret_key).encode(), datestamp)\n    k_region = _sign(k_date, region); k_service = _sign(k_region, 's3'); k_signing = _sign(k_service, 'aws4_request')\n    signature = hmac.new(k_signing, string_to_sign.encode(), hashlib.sha256).hexdigest()\n    auth = f'{algorithm} Credential={access_key}/{credential_scope}, SignedHeaders={signed_headers}, Signature={signature}'\n    url = f'{endpoint}{canonical_uri}'\n    req = urllib.request.Request(url, data=body, method='PUT', headers={\n        'Host': host, 'X-Amz-Date': amzdate, 'X-Amz-Content-SHA256': payload_hash,\n        'Authorization': auth, 'Content-Type': content_type,\n    })\n    with urllib.request.urlopen(req, timeout=60) as resp:\n        return resp.status\n\nchapter_files = sorted([f for f in out_dir.glob('*.txt') if re.match(r'^\\d{3}-', f.name)])\nrefactored, errors, combined_parts = [], [], []\n\nfor f in chapter_files:\n    raw = f.read_text(encoding='utf-8')\n    cache_path = cache_dir / f.name\n    if cache_path.exists() and cache_path.stat().st_size > 0:\n        new_text = cache_path.read_text(encoding='utf-8')\n        cached = True\n    else:\n        new_text = ''\n        last_finish = None\n        vllm_exc = None\n        try:\n            new_text, last_finish = call_vllm(raw)\n        except Exception as e:\n            vllm_exc = f'{type(e).__name__}: {e}'\n        if not new_text:\n            reason = vllm_exc if vllm_exc else f'empty (finish={last_finish})'\n            errors.append({'file': f.name, 'stage': 'vllm-fallback', 'error': f'{reason}; using original'})\n            new_text = raw\n            cache_path.write_text(new_text, encoding='utf-8')\n            cache_path.with_suffix('.fallback').touch()\n            cached = False\n        else:\n            cache_path.write_text(new_text, encoding='utf-8')\n            cached = False\n    body = new_text.encode('utf-8')\n    key = f'{folder}/{f.name}'\n    try:\n        status = s3_put(key, body, 'text/plain; charset=utf-8')\n        refactored.append({'key': key, 'size': len(body), 'cached': cached, 'status': status})\n        combined_parts.append(new_text)\n    except Exception as e:\n        errors.append({'file': f.name, 'stage': 's3', 'error': f'{type(e).__name__}: {e}'})\n\nif combined_parts:\n    combined_text = ''\n    for part in combined_parts:\n        lines = part.split('\\n', 1)\n        head = lines[0].lstrip('# ').strip() if lines else ''\n        body_text = lines[1].strip() if len(lines) > 1 else ''\n        combined_text += f'\\n\\n====== {head} ======\\n\\n{body_text}\\n'\n    body = combined_text.encode('utf-8')\n    key = f'{folder}/{slug or folder}_full.txt'\n    try:\n        status = s3_put(key, body, 'text/plain; charset=utf-8')\n        refactored.append({'key': key, 'size': len(body), 'combined': True, 'status': status})\n    except Exception as e:\n        errors.append({'file': '_full.txt', 'stage': 's3', 'error': f'{type(e).__name__}: {e}'})\n\nreturn {\n    'success': len(errors) == 0 and len(refactored) > 0,\n    'slug': slug,\n    'index': prev.get('index'),\n    'total': prev.get('total'),\n    'folder': folder,\n    'bucket': bucket,\n    'model': model,\n    'refactored_count': len(refactored),\n    'failed_count': len(errors),\n    'errors': errors[:5],\n}\n"
      },
      "id": "refactor-1",
      "name": "Refactor with vLLM (per manga)",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1520,
        300
      ]
    }
  ],
  "connections": {
    "Manual Trigger": {
      "main": [
        [
          {
            "node": "Set Params",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Set Params": {
      "main": [
        [
          {
            "node": "List Manga (sitemap)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "List Manga (sitemap)": {
      "main": [
        [
          {
            "node": "Loop Over Manga",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Loop Over Manga": {
      "main": [
        [],
        [
          {
            "node": "Run Crawler (per manga)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Run Crawler (per manga)": {
      "main": [
        [
          {
            "node": "Upload to MinIO (per manga)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Upload to MinIO (per manga)": {
      "main": [
        [
          {
            "node": "Refactor with vLLM (per manga)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Refactor with vLLM (per manga)": {
      "main": [
        [
          {
            "node": "Loop Over Manga",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}